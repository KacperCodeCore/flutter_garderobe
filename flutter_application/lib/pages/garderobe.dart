import 'package:flutter/material.dart';

class UserGarderobe extends StatelessWidget {
  // const UserGarderobe({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Garderobe')),
    );
  }
}
