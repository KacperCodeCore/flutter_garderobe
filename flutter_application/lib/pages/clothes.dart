import 'package:flutter/material.dart';

class UserClothes extends StatelessWidget {
  // const UserClothes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Clothes')),
    );
  }
}
