import 'package:flutter/material.dart';

class ElementStarCreator extends StatelessWidget {
  const ElementStarCreator({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}
