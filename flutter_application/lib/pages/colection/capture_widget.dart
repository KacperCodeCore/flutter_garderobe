import 'package:flutter/material.dart';

class CaptureWidget extends StatelessWidget {
  final Widget? child;
  const CaptureWidget({super.key, this.child});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
